---
phase: 03-connect-workflow-to-real-neo4j
plan: 03
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - tests/integration/test_query_pipeline.py
  - src/schema/SCHEMA.md
autonomous: false
requirements:
  - QUERY-01
  - QUERY-02
  - QUERY-03
  - QUERY-04

must_haves:
  truths:
    - "All 4 QUERY requirements have passing integration test coverage"
    - "Empty roster edge case returns a result dict (no exception) — graceful degradation works"
    - "End-to-end pipeline invocation (plan -> cypher -> validate -> analyze -> format) completes against live graph"
    - "End-to-end latency baseline is measured and logged (target: <= 15s)"
    - "SCHEMA.md contains a TODO OPT-03 comment documenting the AF zone extension path"
    - "Human confirms: returned synergy pair matches wiki data for known character"
  artifacts:
    - path: "tests/integration/test_query_pipeline.py"
      provides: "Extended tests: empty roster, end-to-end pipeline, latency measurement"
    - path: "src/schema/SCHEMA.md"
      provides: "AF zone mechanics TODO comment (OPT-03 annotation)"
      contains: "TODO OPT-03"
  key_links:
    - from: "tests/integration/test_query_pipeline.py"
      to: "src/workflow/run.py"
      via: "calls run.main(roster, query) and asserts final_output structure"
      pattern: "run\\.main"
---

<objective>
Complete Phase 3 with extended integration coverage — empty roster edge case, end-to-end pipeline test with latency measurement, AF zone schema documentation — then human verifies the known-good synergy pair against wiki.

Purpose: The ROADMAP success criterion #3 requires a known-good synergy pair to be verified manually. Success criterion #4 requires integration tests covering at least 3 known-good query scenarios with latency baseline. This plan satisfies both.

Output: Extended test_query_pipeline.py with 3 scenarios, OPT-03 annotation in SCHEMA.md, human verification of synergy correctness.
</objective>

<execution_context>
@/home/shogunix/.claude/get-shit-done/workflows/execute-plan.md
@/home/shogunix/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/workflow/run.py
@src/workflow/nodes/plan.py
@src/workflow/nodes/validate.py
@tests/integration/test_query_pipeline.py
@tests/conftest.py

# Prior plan outputs
@.planning/phases/03-connect-workflow-to-real-neo4j/03-01-SUMMARY.md
@.planning/phases/03-connect-workflow-to-real-neo4j/03-02-SUMMARY.md
</context>

<interfaces>
<!-- Key contracts from prior plans. -->

From src/workflow/run.py (created in Plan 02):
```python
async def main(roster: list[str], query: str) -> dict:
    """Returns final_output dict from graph.ainvoke(). Handles driver lifecycle."""
```

From ROADMAP Phase 3 success criteria:
- Criterion 3: "A known-good synergy pair (verified manually against the wiki) is returned by the pipeline with correct Grasta and personality attribution"
- Criterion 4: "Integration tests against AuraDB Free (or local Docker Neo4j fallback) pass with at least 3 known-good query scenarios covering different team archetypes"
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add empty-roster and end-to-end pipeline integration tests with latency measurement</name>
  <files>tests/integration/test_query_pipeline.py, src/schema/SCHEMA.md</files>
  <action>
    Add three additional test scenarios to test_query_pipeline.py:

    **Scenario: test_empty_roster_graceful_degradation (QUERY-01, QUERY-02)**
    - roster = [], query = "best team for fire content"
    - full_roster = augment_with_f2p([])  # F2P-only roster
    - Run Cypher: MATCH (c:Character) WHERE c.name IN $roster RETURN c.name
    - Assert: returns non-empty results (F2P characters exist in graph)
    - Assert: all returned names are in F2P_CHARACTERS set

    **Scenario: test_end_to_end_pipeline_with_latency (QUERY-03)**
    - import time
    - roster = ["Aldo", "Ciel"]
    - query = "highest damage blunt zone synergy"
    - start = time.monotonic()
    - result = await run.main(roster, query)
    - elapsed = time.monotonic() - start
    - Assert: result is a dict (not None, no exception)
    - Assert: "error" key not present OR result has team-shaped keys (characters list or similar)
    - Assert: elapsed < 15.0  (15s SLO defined in ROADMAP Phase 5 success criterion #5)
    - Log: print(f"End-to-end latency: {elapsed:.2f}s")  # baseline for Phase 5 reference

    **Scenario: test_grasta_synergy_three_archetypes (QUERY-03)**
    - Three separate Cypher queries covering different team archetypes:
      1. Attack archetype: category='Attack', is_shareable=true — confirm non-empty for Aldo roster
      2. Support archetype: category='Support' — confirm non-empty results exist in graph
      3. Personality-based AF Special: MATCH (c:Character)-[:HAS_TRAIT]->(t:Trait) WHERE c.name IN $roster RETURN c.name, collect(t.name) AS traits — confirm Aldo has traits
    - Each sub-assertion uses pytest.fail with descriptive message if empty

    **SCHEMA.md — add AF zone OPT-03 annotation:**
    Find the SCHEMA.md file (likely at src/schema/SCHEMA.md or SCHEMA.md at project root — check via glob first).
    Add the following comment block to the end of the Relationships section or as a dedicated "Future Extensions" section:
    ```markdown
    ## Future Extensions

    ### OPT-03: AF Zone Mechanics (v2)
    <!-- TODO OPT-03 -->
    AF synergy in Phase 3 operates through existing HAS_TRAIT and REQUIRES_TRAIT paths.
    Zone types are not modeled as nodes. A v2 schema extension would add:
    - `(:Zone {type: "Fire"|"Wind"|"Water"|...})` node label
    - `(Character|Grasta)-[:SETS_ZONE]->(Zone)` relationship
    - `(Character)-[:BENEFITS_FROM_ZONE]->(Zone)` relationship
    This extension is deferred to OPT-03. Phase 3 recommendations use trait matching
    to infer AF zone synergies without explicit zone nodes.
    ```

    SCHEMA.md location check: run `ls /home/shogunix/AnotherEdenAI/src/schema/ 2>/dev/null` and `ls /home/shogunix/AnotherEdenAI/SCHEMA.md 2>/dev/null`. Read the file first, then append the Future Extensions section at the end without modifying existing content.
  </action>
  <verify>
    <automated>cd /home/shogunix/AnotherEdenAI && python -m pytest tests/integration/test_query_pipeline.py -m integration -x -q -v</automated>
  </verify>
  <done>All integration tests pass (original 6 + new 3 = 9+). End-to-end latency logged to stdout. SCHEMA.md contains "TODO OPT-03" comment. Empty roster test confirms F2P characters are returned.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human verifies Phase 3 integration and known-good synergy</name>
  <action>Human runs the integration suite and verifies that a known-good synergy pair returned by the pipeline matches wiki data for Aldo.</action>
  <what-built>
    Full Phase 3 integration suite: name normalization, roster filtering, Grasta synergy traversal, end-to-end pipeline, and latency measurement. The pipeline now runs against live Neo4j data.
  </what-built>
  <how-to-verify>
    1. Run the full test suite:
       ```
       cd /home/shogunix/AnotherEdenAI
       pytest tests/integration/test_query_pipeline.py -m integration -v
       ```
       Expected: All tests green. Note the latency printed by test_end_to_end_pipeline_with_latency.

    2. Verify the known-good synergy by running a live query:
       ```
       python -m src.workflow.run --roster "Aldo,Ciel" --query "blunt zone synergy team"
       ```
       Expected: A JSON result dict. It may be an error dict if LLM creds are not configured — that is acceptable. What to check:
       - No Python exception (unhandled traceback)
       - If LLM_PROVIDER=ollama or anthropic: inspect that Grastas returned in any synergy results match what anothereden.wiki shows for Aldo's traits.

    3. Check SCHEMA.md for the OPT-03 annotation:
       ```
       grep -n "OPT-03" /home/shogunix/AnotherEdenAI/SCHEMA.md 2>/dev/null || grep -rn "OPT-03" /home/shogunix/AnotherEdenAI/src/
       ```
       Expected: One match in the SCHEMA.md file.

    4. Run the full pytest suite to confirm no regressions:
       ```
       pytest tests/ -x -q
       ```
       Expected: All tests green.
  </how-to-verify>
  <verify>
    <automated>cd /home/shogunix/AnotherEdenAI && python -m pytest tests/integration/test_query_pipeline.py -m integration -q</automated>
  </verify>
  <done>Human has typed "approved". All integration tests pass. Known-good synergy result verified against wiki.</done>
  <resume-signal>Type "approved" if all tests pass, or describe any failures to investigate</resume-signal>
</task>

</tasks>

<verification>
Full suite including integration:
`cd /home/shogunix/AnotherEdenAI && python -m pytest tests/ -x -q`

Phase 3 integration tests only:
`cd /home/shogunix/AnotherEdenAI && python -m pytest tests/integration/test_query_pipeline.py -m integration -v`
</verification>

<success_criteria>
1. Integration test suite has 9+ passing tests covering QUERY-01 through QUERY-04
2. At least 3 distinct team archetype scenarios are covered (attack synergy, support synergy, AF trait path)
3. End-to-end latency test completes in under 15s and logs the measured time (ROADMAP Phase 3 criterion #4)
4. SCHEMA.md contains OPT-03 annotation documenting AF zone extension path for v2
5. Human has confirmed: known-good synergy query returns plausible Grasta + personality data
6. `pytest tests/ -x -q` passes with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/03-connect-workflow-to-real-neo4j/03-03-SUMMARY.md`
</output>
