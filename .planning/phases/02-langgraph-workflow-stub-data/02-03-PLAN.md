---
phase: 02-langgraph-workflow-stub-data
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/workflow/nodes/validate.py
  - tests/workflow/test_validate.py
  - tests/workflow/test_graph.py
autonomous: true
requirements:
  - AGENT-03
  - AGENT-04
  - AGENT-05

must_haves:
  truths:
    - "VALIDATE executes Cypher via driver.execute_query() -- not a static linter"
    - "Execution exception (mode A) appends formatted error to validation_errors and increments retry_count"
    - "Empty result set (mode B) appends formatted error to validation_errors and increments retry_count"
    - "Success returns db_results with query records and does NOT increment retry_count"
    - "Error message format matches: Attempt {N}: Query failed due to [Exception/Empty Result]. Context: [detail]"
    - "Graph routes failed validate to generate_cypher when retry_count < 3"
    - "Graph routes failed validate to format when retry_count >= 3 (never attempt 4)"
    - "retry_count never exceeds 3"
  artifacts:
    - path: "src/workflow/nodes/validate.py"
      provides: "VALIDATE node executing Cypher and trapping both failure modes"
      contains: "execute_query"
      min_lines: 25
    - path: "tests/workflow/test_validate.py"
      provides: "Unit tests for both failure modes and success path"
      min_lines: 50
  key_links:
    - from: "src/workflow/nodes/validate.py"
      to: "driver.execute_query"
      via: "closure-injected driver parameter"
      pattern: "driver.execute_query"
    - from: "src/workflow/graph.py"
      to: "src/workflow/nodes/validate.py"
      via: "lambda closure in build_graph"
      pattern: "lambda.*validate_node.*driver"
---

<objective>
Implement the VALIDATE node with real execution logic (Cypher execution against driver, trapping both failure modes) and update graph routing tests to work with the real validate implementation.

Purpose: VALIDATE is the critical safety gate -- it prevents hallucinated Cypher from reaching ANALYZE. The retry loop is the most complex control flow in the pipeline and must be tested at both unit and graph levels.

Output: Working VALIDATE node with both failure modes, updated graph routing tests confirming retry behavior.
</objective>

<execution_context>
@/home/shogunix/.claude/get-shit-done/workflows/execute-plan.md
@/home/shogunix/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-langgraph-workflow-stub-data/02-CONTEXT.md
@.planning/phases/02-langgraph-workflow-stub-data/02-RESEARCH.md
@.planning/phases/02-langgraph-workflow-stub-data/02-01-SUMMARY.md
@.planning/phases/02-langgraph-workflow-stub-data/02-02-SUMMARY.md
@src/workflow/state.py
@src/workflow/graph.py
@src/workflow/nodes/validate.py
</context>

<interfaces>
<!-- From plan 02-01 outputs -->
From src/workflow/state.py:
```python
class WorkflowState(TypedDict):
    user_query: str
    roster: list[str]
    plan_strategy: str
    cypher_query: str
    db_results: list[dict]
    validation_errors: Annotated[list[str], operator.add]
    retry_count: int
    analysis_result: str
    final_output: dict
```

From src/workflow/graph.py:
```python
def build_graph(driver=None) -> CompiledGraph: ...
def route_after_validate(state: WorkflowState) -> Literal["generate_cypher", "analyze", "format"]: ...
```

From tests/workflow/conftest.py:
```python
@pytest.fixture
def stub_driver(): ...  # MagicMock for driver.execute_query()
@pytest.fixture
def sample_state(): ...  # Complete WorkflowState dict
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VALIDATE node implementation and unit tests</name>
  <files>src/workflow/nodes/validate.py, tests/workflow/test_validate.py</files>
  <behavior>
    - Test: validate_node success path returns {"db_results": [records]} with no retry_count increment
    - Test: validate_node returns only db_results key on success (AGENT-07)
    - Test: validate_node exception (mode A) returns {"validation_errors": [formatted_msg], "retry_count": N+1}
    - Test: validate_node empty result (mode B) returns {"validation_errors": [formatted_msg], "retry_count": N+1}
    - Test: error message contains "Attempt {N}" prefix
    - Test: error message contains "Exception" or "Empty Result" indicator
    - Test: exception error message includes exception type and message
    - Test: validate_node does NOT return db_results on failure (only error keys)
  </behavior>
  <action>
1. Write `tests/workflow/test_validate.py` first (RED phase):
   - `test_validate_success_returns_db_results`: stub_driver returns `([{"name": "Aldo"}], None, None)`. Call `validate_node(state, stub_driver)`. Assert result has `db_results` key with records, does NOT have `retry_count` or `validation_errors` keys.
   - `test_validate_success_returns_only_db_results`: Assert `list(result.keys()) == ["db_results"]` on success (AGENT-07).
   - `test_validate_exception_returns_error`: stub_driver raises `Exception("Unknown label")`. Assert result has `validation_errors` (list of 1 str) and `retry_count` (incremented).
   - `test_validate_exception_error_format`: Assert error string matches `"Attempt 1: Query failed due to Exception. Context: Exception: Unknown label"` pattern.
   - `test_validate_empty_result_returns_error`: stub_driver returns `([], None, None)`. Assert same error structure as exception but with "Empty Result" in message.
   - `test_validate_increments_retry_from_current`: Set `retry_count=2` in state, trigger failure. Assert result `retry_count == 3`.
   - `test_validate_failure_does_not_return_db_results`: On failure, assert `"db_results"` NOT in result keys.

2. Implement `src/workflow/nodes/validate.py` (GREEN phase):
   - `RETRY_CAP = 3` constant.
   - `validate_node(state: WorkflowState, driver) -> dict`:
     - Read `retry_count = state.get("retry_count", 0)` and `cypher = state["cypher_query"]`.
     - Try `records, _, _ = driver.execute_query(cypher, database_="neo4j")`.
     - On exception: format error `f"Attempt {retry_count + 1}: Query failed due to Exception. Context: {type(exc).__name__}: {exc}"`. Return `{"validation_errors": [error_msg], "retry_count": retry_count + 1}`.
     - On empty records (`not records`): format error `f"Attempt {retry_count + 1}: Query failed due to Empty Result. Context: Query returned no records — possible hallucinated traversal path."`. Return `{"validation_errors": [error_msg], "retry_count": retry_count + 1}`.
     - On success: Return `{"db_results": [dict(r) for r in records]}` (convert records to plain dicts). Only return `db_results` -- no other keys.
   - Note: `driver` is injected via closure from `graph.py`'s `build_graph(driver)`. Tests call `validate_node(state, mock_driver)` directly.
  </action>
  <verify>
    <automated>cd /home/shogunix/AnotherEdenAI && pytest tests/workflow/test_validate.py -x --tb=short</automated>
  </verify>
  <done>
    - VALIDATE executes Cypher via driver.execute_query()
    - Mode A (exception) correctly formats error and increments retry_count
    - Mode B (empty result) correctly formats error and increments retry_count
    - Success returns only db_results with no error keys
    - Error format matches the locked template exactly
  </done>
</task>

<task type="auto">
  <name>Task 2: Update graph routing tests for real validate logic</name>
  <files>tests/workflow/test_graph.py</files>
  <action>
Update `tests/workflow/test_graph.py` to work with the real validate_node (no longer a stub). The graph tests from plan 02-01 used stub nodes. Now that plan_node and generate_cypher_node have real LLM logic (from 02-02) and validate_node has real driver logic, the graph tests must patch LLMs and use mock drivers.

1. Update `test_full_graph_happy_path`:
   - Patch `ChatAnthropic` in all LLM-using node modules (`src.workflow.nodes.plan`, `src.workflow.nodes.cypher`, `src.workflow.nodes.analyze`).
   - Use `mock_llm.invoke.side_effect` with appropriate `AIMessage` responses for each node call.
   - Use `build_graph(driver=stub_driver)` where stub_driver returns non-empty results.
   - Invoke the compiled graph. Assert final_output has the expected schema.

2. Update `test_single_retry_routes_back_to_generate_cypher`:
   - stub_driver.execute_query with `side_effect=[([], None, None), ([{"name": "Aldo"}], None, None)]`.
   - Mock LLM with enough responses for: PLAN (1), GENERATE_CYPHER (2 calls due to retry), ANALYZE (1).
   - Assert retry_count == 1, len(validation_errors) == 1, final_output is not an error.

3. Update `test_retry_cap_exhausted_routes_to_format_error`:
   - stub_driver always returns `([], None, None)`.
   - Mock LLM with responses for: PLAN (1), GENERATE_CYPHER (3 calls).
   - Assert retry_count == 3, len(validation_errors) == 3, `"error"` in final_output.
   - Assert driver.execute_query called exactly 3 times (never a 4th).

4. Keep the `test_route_after_validate_*` unit tests unchanged (they test the router function directly).

NOTE on analyze_node: It still returns a stub at this point (will be replaced in plan 02-04). The graph tests work because format_node reads analysis_result from state and produces final_output. If analyze_node returns `{"analysis_result": "stub"}`, format_node should still produce a valid final_output dict.
  </action>
  <verify>
    <automated>cd /home/shogunix/AnotherEdenAI && pytest tests/workflow/test_graph.py -x --tb=short</automated>
  </verify>
  <done>
    - Graph happy path test passes with mocked LLMs and mock driver
    - Single retry test confirms routing back to generate_cypher works
    - Retry cap test confirms cap at 3, never 4th attempt
    - All workflow tests pass: `pytest tests/workflow/ -x --tb=short`
  </done>
</task>

</tasks>

<verification>
- `pytest tests/workflow/test_validate.py -x --tb=short` — validate unit tests pass
- `pytest tests/workflow/test_graph.py -x --tb=short` — graph routing tests pass
- `pytest tests/workflow/ -x --tb=short` — full workflow suite passes
</verification>

<success_criteria>
1. VALIDATE executes Cypher via driver, traps exceptions AND empty results
2. Error messages follow the locked format exactly
3. Graph routing: pass->analyze, fail+retry<3->generate_cypher, fail+retry>=3->format
4. retry_count never exceeds 3, driver.execute_query never called more than 3 times on cap path
5. All workflow tests pass with mocked LLMs and drivers
</success_criteria>

<output>
After completion, create `.planning/phases/02-langgraph-workflow-stub-data/02-03-SUMMARY.md`
</output>
