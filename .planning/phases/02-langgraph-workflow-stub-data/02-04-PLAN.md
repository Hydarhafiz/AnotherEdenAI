---
phase: 02-langgraph-workflow-stub-data
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - src/workflow/nodes/analyze.py
  - src/workflow/nodes/format.py
  - tests/workflow/test_analyze.py
  - tests/workflow/test_format.py
  - tests/workflow/test_graph.py
autonomous: true
requirements:
  - AGENT-06
  - AGENT-07

must_haves:
  truths:
    - "ANALYZE node reads db_results and writes analysis_result (only)"
    - "ANALYZE uses Sonnet 4.6 to synthesize team recommendation from db_results"
    - "FORMAT node reads analysis_result and writes final_output (only)"
    - "FORMAT produces structured dict with frontline, reserve, synergy_explanation"
    - "FORMAT on error path (retry_count >= 3, no db_results) produces error schema with same keys"
    - "FORMAT validates output with Pydantic v2 TeamOutput model"
    - "Full graph test passes end-to-end with all nodes mocked -- no live LLM or Neo4j"
  artifacts:
    - path: "src/workflow/nodes/analyze.py"
      provides: "ANALYZE node with Sonnet 4.6 team synthesis"
      contains: "ChatAnthropic"
      min_lines: 20
    - path: "src/workflow/nodes/format.py"
      provides: "FORMAT node with Pydantic validation and error path"
      contains: "TeamOutput"
      min_lines: 30
    - path: "tests/workflow/test_analyze.py"
      provides: "ANALYZE unit tests with mocked LLM"
      min_lines: 25
    - path: "tests/workflow/test_format.py"
      provides: "FORMAT unit tests for success and error paths"
      min_lines: 40
  key_links:
    - from: "src/workflow/nodes/analyze.py"
      to: "state['db_results']"
      via: "reads query results"
      pattern: "db_results"
    - from: "src/workflow/nodes/format.py"
      to: "state['analysis_result']"
      via: "reads ANALYZE output text"
      pattern: "analysis_result"
    - from: "src/workflow/nodes/format.py"
      to: "TeamOutput Pydantic model"
      via: "model_validate"
      pattern: "TeamOutput"
---

<objective>
Implement ANALYZE and FORMAT nodes with full logic, completing the pipeline. ANALYZE synthesizes db_results into a team recommendation via Sonnet 4.6. FORMAT transforms the analysis into a structured dict validated by Pydantic v2. Final full-graph end-to-end tests confirm the entire pipeline works with mocked dependencies.

Purpose: These nodes complete the five-node pipeline. FORMAT is the final output boundary where Pydantic validates the team recommendation schema that Phase 4 (web layer) will consume directly.

Output: Complete, tested pipeline. All workflow tests pass. Phase 2 is ready for verification.
</objective>

<execution_context>
@/home/shogunix/.claude/get-shit-done/workflows/execute-plan.md
@/home/shogunix/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-langgraph-workflow-stub-data/02-CONTEXT.md
@.planning/phases/02-langgraph-workflow-stub-data/02-RESEARCH.md
@.planning/phases/02-langgraph-workflow-stub-data/02-01-SUMMARY.md
@.planning/phases/02-langgraph-workflow-stub-data/02-02-SUMMARY.md
@.planning/phases/02-langgraph-workflow-stub-data/02-03-SUMMARY.md
@src/workflow/state.py
@src/workflow/graph.py
@src/workflow/nodes/analyze.py
@src/workflow/nodes/format.py
</context>

<interfaces>
<!-- From prior plan outputs -->
From src/workflow/state.py:
```python
class WorkflowState(TypedDict):
    user_query: str
    roster: list[str]
    plan_strategy: str
    cypher_query: str
    db_results: list[dict]
    validation_errors: Annotated[list[str], operator.add]
    retry_count: int
    analysis_result: str
    final_output: dict
```

From src/workflow/nodes/format.py (research Pattern 5 -- target structure):
```python
class CharacterSlot(BaseModel):
    name: str
    role: str
    grastas: list[str]

class TeamOutput(BaseModel):
    frontline: list[CharacterSlot]
    reserve: list[CharacterSlot]
    synergy_explanation: str
    error: Optional[str] = None
```

From tests/workflow/conftest.py:
```python
@pytest.fixture
def stub_driver(): ...
@pytest.fixture
def mock_llm(): ...
@pytest.fixture
def sample_state(): ...
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ANALYZE and FORMAT node implementation with unit tests</name>
  <files>
    src/workflow/nodes/analyze.py
    src/workflow/nodes/format.py
    tests/workflow/test_analyze.py
    tests/workflow/test_format.py
  </files>
  <behavior>
    ANALYZE:
    - Test: analyze_node returns dict with only "analysis_result" key (AGENT-07)
    - Test: analyze_node passes db_results, user_query, roster, and plan_strategy to LLM
    - Test: analyze_node uses Sonnet 4.6
    - Test: analysis_result is the LLM response content string

    FORMAT (success path):
    - Test: format_node returns dict with only "final_output" key (AGENT-07)
    - Test: format_node reads analysis_result from state and produces structured dict
    - Test: final_output has frontline (list), reserve (list), synergy_explanation (str)
    - Test: final_output is validated by Pydantic TeamOutput model
    - Test: each item in frontline/reserve has name, role, grastas keys

    FORMAT (error path):
    - Test: when retry_count >= 3 and db_results is empty, format_node returns error schema
    - Test: error schema has error (str), frontline ([]), reserve ([]), synergy_explanation ("")
    - Test: error string contains the validation_errors joined
  </behavior>
  <action>
1. Write `tests/workflow/test_analyze.py` (RED):
   - `test_analyze_returns_only_analysis_result`: Patch `get_llm` in `src.workflow.nodes.analyze`, call analyze_node, assert `list(result.keys()) == ["analysis_result"]`.
   - `test_analyze_passes_db_results_to_llm`: Verify HumanMessage or SystemMessage content includes stringified db_results.
   - `test_analyze_uses_analyzer_role`: Verify `get_llm` was called with `role="analyzer"`.
   - `test_analyze_result_is_llm_content`: Assert `result["analysis_result"] == mock_response.content`.

2. Write `tests/workflow/test_format.py` (RED):
   - `test_format_success_returns_only_final_output`: Provide state with `analysis_result` containing a valid JSON team recommendation string. Assert `list(result.keys()) == ["final_output"]`.
   - `test_format_success_has_required_keys`: Assert final_output dict has frontline, reserve, synergy_explanation.
   - `test_format_success_validates_with_pydantic`: Import TeamOutput, call `TeamOutput.model_validate(result["final_output"])` -- should not raise.
   - `test_format_error_path_on_retry_cap`: Provide state with `retry_count=3`, `db_results=[]`, `validation_errors=["err1", "err2", "err3"]`, `analysis_result=""`. Assert final_output has `"error"` key, frontline is [], reserve is [].
   - `test_format_error_contains_validation_errors`: Assert the error string contains the validation_errors content.

3. Implement `src/workflow/nodes/analyze.py` (GREEN):
   - Import `get_llm` from `..llm`, `HumanMessage`, `SystemMessage` from `langchain_core.messages`.
   - Define `ANALYZE_SYSTEM_PROMPT`: Instruct LLM to synthesize the db_results into a team recommendation. Tell it to output a JSON object with structure: `{"frontline": [{"name": str, "role": str, "grastas": [str]}], "reserve": [...], "synergy_explanation": str}`. Emphasize: use only characters from the db_results, assign roles (AF anchor, healer, DPS, support, etc.), explain Grasta synergies.
   - `analyze_node(state: WorkflowState) -> dict`:
     - Call `llm = get_llm(role="analyzer")` inside function.
     - Build messages with db_results, user_query, roster, plan_strategy in the prompt.
     - Return `{"analysis_result": response.content}`.

4. Implement `src/workflow/nodes/format.py` (GREEN):
   - Define Pydantic models: `CharacterSlot(BaseModel)` with name, role, grastas fields. `TeamOutput(BaseModel)` with frontline, reserve, synergy_explanation, optional error.
   - `format_node(state: WorkflowState) -> dict`:
     - Check error path: if `retry_count >= 3` and `not state.get("db_results")`:
       - Return `{"final_output": {"error": "; ".join(state.get("validation_errors", [])), "frontline": [], "reserve": [], "synergy_explanation": ""}}`.
     - Happy path: parse `analysis_result` as JSON. If JSON parsing fails, attempt to extract JSON from the text (LLM might include preamble). Use `json.loads()` with fallback regex for JSON block extraction.
     - Validate parsed dict with `TeamOutput.model_validate(parsed)`.
     - Return `{"final_output": validated.model_dump()}`.
   - Note: FORMAT is intentionally LLM-free -- pure Python transformation. This makes it deterministic and easily testable.
  </action>
  <verify>
    <automated>cd /home/shogunix/AnotherEdenAI && pytest tests/workflow/test_analyze.py tests/workflow/test_format.py -x --tb=short</automated>
  </verify>
  <done>
    - ANALYZE reads db_results, calls Sonnet 4.6, returns only analysis_result
    - FORMAT parses analysis_result JSON into structured TeamOutput dict
    - FORMAT error path produces same schema with error key and empty lists
    - Pydantic v2 validates all FORMAT output
    - All analyze and format tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Final end-to-end graph tests with all real nodes</name>
  <files>tests/workflow/test_graph.py</files>
  <action>
Update `tests/workflow/test_graph.py` to be the definitive end-to-end test suite now that all nodes have real logic. This is the final validation that the complete pipeline works.

1. Update `test_full_graph_happy_path`:
   - Mock LLM responses for all 3 LLM-using nodes (plan, cypher, analyze):
     - PLAN: `AIMessage(content="Find Fire element characters with synergy traits")`
     - GENERATE_CYPHER: `AIMessage(content="MATCH (c:Character)-[:HAS_TRAIT]->(t:Trait) WHERE c.name IN $roster RETURN c.name, t.name")`
     - ANALYZE: `AIMessage(content='{"frontline": [{"name": "Aldo", "role": "DPS", "grastas": ["Fire T3"]}], "reserve": [{"name": "Ciel", "role": "support", "grastas": []}], "synergy_explanation": "Fire synergy with Aldo as main DPS"}')`
   - stub_driver returns `([{"c.name": "Aldo", "t.name": "Straw Dummy"}], None, None)`.
   - Patch `get_llm` in all 3 node modules (`src.workflow.nodes.plan`, `src.workflow.nodes.cypher`, `src.workflow.nodes.analyze`).
   - Build graph with `build_graph(driver=stub_driver)`.
   - Invoke with sample initial state.
   - Assert: `final_output` has `frontline` with Aldo, `reserve` with Ciel, non-empty `synergy_explanation`. No `error` key (or error is None).
   - Assert: `retry_count == 0`, `validation_errors == []`.

2. Update `test_single_retry_routes_back_to_generate_cypher`:
   - stub_driver.execute_query with `side_effect=[([], None, None), ([{"name": "Aldo"}], None, None)]` (fail then succeed).
   - Mock LLM with enough `side_effect` responses for: PLAN(1) + CYPHER(2, since retry) + ANALYZE(1) = 4 LLM invocations total across the mocks.
   - Patch each module's `get_llm` separately with its own mock to control response ordering per node.
   - Assert: retry_count == 1, len(validation_errors) == 1, final_output has team data (not error).

3. Update `test_retry_cap_exhausted_routes_to_format_error`:
   - stub_driver always returns `([], None, None)`.
   - Mock LLM for: PLAN(1) + CYPHER(3, all retries) = 4 total LLM invocations. ANALYZE is never called.
   - Assert: retry_count == 3, len(validation_errors) == 3, `"error"` in final_output, frontline == [], reserve == [].
   - Assert: stub_driver.execute_query.call_count == 3 (never 4th attempt).

4. Add `test_full_pipeline_no_live_calls`:
   - Smoke test that runs the full graph and asserts no `ANTHROPIC_API_KEY` env var is needed.
   - This confirms AGENT requirement: "pytest passes with all nodes mocked -- no live LLM calls, no live Neo4j."

5. Run the full test suite (not just workflow) to ensure no regressions:
   - `pytest tests/ --ignore=tests/integration -x --tb=short`
  </action>
  <verify>
    <automated>cd /home/shogunix/AnotherEdenAI && pytest tests/workflow/ -x --tb=short && pytest tests/ --ignore=tests/integration -x --tb=short</automated>
  </verify>
  <done>
    - Full graph happy path: query flows through all 5 nodes, returns structured team output
    - Single retry: VALIDATE failure routes to GENERATE_CYPHER, second attempt succeeds
    - Retry cap: 3 failures route to FORMAT graceful error, never 4th attempt
    - No live LLM or Neo4j calls required for any test
    - Full test suite passes (workflow + unit, excluding integration)
    - Phase 2 is complete and ready for /gsd:verify-work
  </done>
</task>

</tasks>

<verification>
- `pytest tests/workflow/ -x --tb=short` — all workflow tests pass
- `pytest tests/ --ignore=tests/integration -x --tb=short` — no regressions in existing tests
- `python -c "from src.workflow import compiled_graph; print(type(compiled_graph))"` — graph importable
</verification>

<success_criteria>
1. ANALYZE reads db_results, calls Sonnet 4.6, writes analysis_result only
2. FORMAT transforms analysis_result into Pydantic-validated TeamOutput dict
3. FORMAT error path produces same schema with error key for web layer compatibility
4. Full pipeline test: happy path, single retry, retry cap -- all pass
5. No live LLM calls, no live Neo4j connections needed for test suite
6. No regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/02-langgraph-workflow-stub-data/02-04-SUMMARY.md`
</output>
