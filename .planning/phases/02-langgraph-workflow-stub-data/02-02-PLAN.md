---
phase: 02-langgraph-workflow-stub-data
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/workflow/nodes/plan.py
  - src/workflow/nodes/cypher.py
  - tests/workflow/test_plan.py
  - tests/workflow/test_cypher.py
autonomous: true
requirements:
  - AGENT-01
  - AGENT-02

must_haves:
  truths:
    - "PLAN node calls get_llm(role='planner') from src.workflow.llm — no direct ChatAnthropic import"
    - "PLAN node returns only {'plan_strategy': str}"
    - "GENERATE_CYPHER node injects full schema from SCHEMA.md into system prompt"
    - "GENERATE_CYPHER node includes few-shot Cypher examples in prompt"
    - "GENERATE_CYPHER node calls get_llm(role='cypher') from src.workflow.llm"
    - "GENERATE_CYPHER node returns only {'cypher_query': str}"
    - "Cypher output is stripped of markdown code fences if present"
  artifacts:
    - path: "src/workflow/nodes/plan.py"
      provides: "PLAN node with Sonnet 4.6 LLM call"
      contains: "ChatAnthropic"
      min_lines: 20
    - path: "src/workflow/nodes/cypher.py"
      provides: "GENERATE_CYPHER node with schema injection and few-shot examples"
      contains: "get_schema"
      min_lines: 40
    - path: "tests/workflow/test_plan.py"
      provides: "PLAN node unit tests with mocked LLM"
      min_lines: 30
    - path: "tests/workflow/test_cypher.py"
      provides: "GENERATE_CYPHER unit tests with mocked LLM and schema"
      min_lines: 40
  key_links:
    - from: "src/workflow/nodes/plan.py"
      to: "src/workflow/llm.py"
      via: "get_llm(role='planner')"
      pattern: "get_llm"
    - from: "src/workflow/nodes/cypher.py"
      to: "src/workflow/llm.py"
      via: "get_llm(role='cypher')"
      pattern: "get_llm"
    - from: "src/workflow/nodes/cypher.py"
      to: "SCHEMA.md"
      via: "get_schema() or loaded schema string"
      pattern: "schema"
---

<objective>
Implement the PLAN and GENERATE_CYPHER nodes with full LLM logic, replacing their stubs.

Purpose: These two nodes form the first half of the pipeline -- decomposing user queries into strategy and generating Cypher queries. They must be tested with mocked LLM calls to verify prompt construction and output parsing.

Output: Working PLAN and GENERATE_CYPHER nodes with unit tests, all mocked.
</objective>

<execution_context>
@/home/shogunix/.claude/get-shit-done/workflows/execute-plan.md
@/home/shogunix/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-langgraph-workflow-stub-data/02-CONTEXT.md
@.planning/phases/02-langgraph-workflow-stub-data/02-RESEARCH.md
@.planning/phases/02-langgraph-workflow-stub-data/02-01-SUMMARY.md
@SCHEMA.md
@src/workflow/state.py
@src/workflow/graph.py
</context>

<interfaces>
<!-- From plan 02-01 outputs -->
From src/workflow/state.py:
```python
class WorkflowState(TypedDict):
    user_query: str
    roster: list[str]
    plan_strategy: str
    cypher_query: str
    db_results: list[dict]
    validation_errors: Annotated[list[str], operator.add]
    retry_count: int
    analysis_result: str
    final_output: dict
```

From tests/workflow/conftest.py:
```python
@pytest.fixture
def stub_driver(): ...  # MagicMock for driver.execute_query()

@pytest.fixture
def mock_llm(): ...  # MagicMock for ChatAnthropic.invoke()

@pytest.fixture
def sample_state(): ...  # Complete WorkflowState dict with defaults
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PLAN node implementation with unit tests</name>
  <files>src/workflow/nodes/plan.py, tests/workflow/test_plan.py</files>
  <behavior>
    - Test: plan_node returns dict with only "plan_strategy" key (AGENT-07 key ownership)
    - Test: plan_node passes user_query and roster to LLM in HumanMessage
    - Test: plan_node uses SystemMessage with instructions to decompose into graph traversal sub-goals
    - Test: plan_node calls ChatAnthropic with model="claude-sonnet-4-6"
    - Test: plan_strategy content comes from LLM response.content
  </behavior>
  <action>
1. Replace `tests/workflow/test_plan.py` placeholder with full tests:
   - `test_plan_node_returns_only_plan_strategy`: Patch `get_llm` in `src.workflow.nodes.plan`, invoke plan_node with sample_state, assert `list(result.keys()) == ["plan_strategy"]`.
   - `test_plan_node_passes_query_and_roster`: Verify the HumanMessage content includes both `user_query` and roster character names.
   - `test_plan_node_uses_get_llm_with_planner_role`: Verify `get_llm` was called with `role="planner"`.
   - All tests use `unittest.mock.patch("src.workflow.nodes.plan.get_llm", return_value=mock_llm)` — patches the factory, not ChatAnthropic directly.

2. Replace `src/workflow/nodes/plan.py` stub with full implementation:
   - Import `get_llm` from `..llm`, `HumanMessage`/`SystemMessage` from `langchain_core.messages`.
   - Define `PLAN_SYSTEM_PROMPT` constant: instruct the LLM to decompose the user's team-building query into graph traversal sub-goals. The prompt should mention: identify elements, weapons, personality traits needed; determine which Character-Trait-Grasta paths to explore; consider roster constraints. Keep the prompt focused -- this is strategy decomposition, not Cypher generation.
   - `plan_node(state: WorkflowState) -> dict`: Call `llm = get_llm(role="planner")` INSIDE the function. Construct messages. Invoke. Return `{"plan_strategy": response.content}`.
  </action>
  <verify>
    <automated>cd /home/shogunix/AnotherEdenAI && pytest tests/workflow/test_plan.py -x --tb=short</automated>
  </verify>
  <done>
    - plan_node calls Sonnet 4.6 with user_query and roster
    - plan_node returns only {"plan_strategy": str}
    - All tests pass with mocked LLM (no live API calls)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GENERATE_CYPHER node with schema injection, few-shot examples, and unit tests</name>
  <files>src/workflow/nodes/cypher.py, tests/workflow/test_cypher.py</files>
  <behavior>
    - Test: generate_cypher_node returns dict with only "cypher_query" key
    - Test: system prompt includes the full graph schema string
    - Test: system prompt includes few-shot Cypher examples (at least the 4 examples from research)
    - Test: system prompt includes constraints (no ENHANCES, Ore standalone, VC no REQUIRES_TRAIT)
    - Test: plan_strategy from state is included in the prompt
    - Test: validation_errors from previous failures are included in prompt when non-empty (for retries)
    - Test: markdown code fences are stripped from LLM output
  </behavior>
  <action>
1. Replace `tests/workflow/test_cypher.py` placeholder with full tests:
   - `test_cypher_node_returns_only_cypher_query`: Assert key ownership.
   - `test_cypher_node_injects_schema`: Verify `SystemMessage` content includes schema labels (Character, Grasta, Trait, Ore) and relationship types (HAS_TRAIT, REQUIRES_TRAIT).
   - `test_cypher_node_includes_few_shot_examples`: Verify system prompt contains at least one `MATCH` example.
   - `test_cypher_node_includes_plan_strategy`: Verify HumanMessage includes `state["plan_strategy"]`.
   - `test_cypher_node_includes_validation_errors_on_retry`: Set `validation_errors=["Attempt 1: error"]` in state, verify this appears in the prompt so the LLM can correct its query.
   - `test_cypher_node_strips_markdown_fences`: Mock LLM returning `` ```cypher\nMATCH (n) RETURN n\n``` ``, assert result `cypher_query` is clean `MATCH (n) RETURN n`.
   - All tests use `unittest.mock.patch("src.workflow.nodes.cypher.get_llm", ...)`.

2. Replace `src/workflow/nodes/cypher.py` stub with full implementation:
   - Import `get_llm` from `..llm`.
   - Define `SCHEMA_CONTEXT` as a module-level constant string containing the SCHEMA.md content. Hardcode it (not file read) -- it's a stable contract from Phase 1. Include all node labels, properties, relationship types, and the NOTE about Ore being standalone.
   - Define `FEW_SHOT_EXAMPLES` constant with the 4 examples from research (fire characters, trait lookup, shareable grasta by trait, attack grastas by roster). Add constraints: "Never use ENHANCES relationship", "Ore nodes have no relationships", "VC Grastas have no REQUIRES_TRAIT edges".
   - Define `CYPHER_SYSTEM_PROMPT` that combines schema context, few-shot examples, and instructions: "Generate a single Cypher query. Use $roster as the parameter for the user's character list. Return raw Cypher only, no markdown formatting."
   - `generate_cypher_node(state: WorkflowState) -> dict`:
     - Call `llm = get_llm(role="cypher")` inside function.
     - Build HumanMessage from `plan_strategy` + `user_query` + `roster`.
     - If `validation_errors` is non-empty, append the error history to HumanMessage so the LLM can see what went wrong previously.
     - Invoke LLM.
     - Strip markdown fences from response: `response.content.strip().strip("```cypher").strip("```").strip()`.
     - Return `{"cypher_query": cleaned_cypher}`.
  </action>
  <verify>
    <automated>cd /home/shogunix/AnotherEdenAI && pytest tests/workflow/test_cypher.py -x --tb=short</automated>
  </verify>
  <done>
    - generate_cypher_node injects full schema and few-shot examples
    - generate_cypher_node includes validation_errors in prompt on retry
    - generate_cypher_node strips markdown code fences
    - generate_cypher_node returns only {"cypher_query": str}
    - All tests pass with mocked LLM
  </done>
</task>

</tasks>

<verification>
- `pytest tests/workflow/test_plan.py tests/workflow/test_cypher.py -x --tb=short` — all pass
- `pytest tests/workflow/ -x --tb=short` — full workflow suite still passes (including graph tests from 02-01)
</verification>

<success_criteria>
1. PLAN node calls Sonnet 4.6 with query + roster, returns only plan_strategy
2. GENERATE_CYPHER node injects full SCHEMA.md content and 4 few-shot examples into prompt
3. GENERATE_CYPHER includes validation_errors on retry so LLM can self-correct
4. Markdown code fences are stripped from Cypher output
5. Both nodes tested with mocked LLM -- no live API calls
6. All workflow tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-langgraph-workflow-stub-data/02-02-SUMMARY.md`
</output>
