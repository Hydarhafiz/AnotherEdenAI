---
phase: 3.1
slug: cloudflare-bypass-nodriver
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-19
---

# Phase 3.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (existing) |
| **Config file** | `pytest.ini` |
| **Quick run command** | `pytest tests/unit/test_scraper.py -x -q` |
| **Full suite command** | `pytest -x -q --ignore=tests/integration` |
| **Estimated runtime** | ~5 seconds (unit); ~30-60s (integration, requires live wiki) |

---

## Sampling Rate

- **After every task commit:** Run `pytest tests/unit/test_scraper.py -x -q`
- **After every plan wave:** Run `pytest -x -q --ignore=tests/integration`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 10 seconds (unit suite)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 3.1-01-01 | 01 | 1 | SCRAPE-01, SCRAPE-02 | — | N/A | unit | `pytest tests/unit/test_scraper.py -x -q` | ✅ existing | ⬜ pending |
| 3.1-01-02 | 01 | 1 | SCRAPE-03 | — | N/A | unit | `pytest tests/unit/test_scraper.py::test_parse_character_overrides -x -q` | ❌ W0 | ⬜ pending |
| 3.1-01-03 | 01 | 2 | SCRAPE-01, SCRAPE-02, SCRAPE-03 | — | N/A | integration | `pytest tests/integration/test_etl_scraper.py -x -q -m integration` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/unit/test_scraper.py` — add `test_parse_character_overrides` test (fixture-based, no browser; asserts Anabel ES weapon=Spear, Mazrika weapon=Axe)
- [ ] `tests/integration/test_etl_scraper.py` — stub file with `@pytest.mark.integration` marker; tests real nodriver scrape of at least 1 wiki page

*Existing `tests/unit/test_scraper.py` covers parse functions — keep all passing tests.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| All 7 pages return valid HTML via nodriver (Cloudflare bypass works) | SCRAPE-01 | Requires live Cloudflare-protected wiki; cannot mock | Run `python -m src.etl.run_etl` and confirm no 403/429 errors in logs |
| Post-ETL node counts match EXPECTED_NODE_COUNTS | SCRAPE-03 | Requires live Neo4j + live wiki | Run ETL, then `python -c "from src.etl.run_etl import assert_counts; assert_counts()"` exits 0 |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 10s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
